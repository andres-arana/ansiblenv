---
- name: Install autojump
  apt: name=autojump state=latest
  sudo: yes

- name: Install the autojump initializer
  copy: content=". /usr/share/autojump/autojump.sh" dest=~/.bash.d/autojump.bash force=yes
